---
title: "Project 2 code"
author: "Lauren Markwart"
date: "2023-03-20"
output: html_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```

```{r}
library(keras)
library(readbitmap)
library(rlang)
install.packages("Rtools")
install.packages("ImageMagick")
```
```{r}
setwd("/Users/Student/OneDrive/Documents/Fourth Year/DS 4002/dataset2/dataset")
folderMD<-("/Users/Student/OneDrive/Documents/Fourth Year/DS 4002/dataset2/dataset/MildDemented")
filesMD <- list.files(path = folderMD, pattern = "*.jpg", full.names=TRUE)
imagesMD<-lapply(filesMD, load.image)

```
## R Markdown

This is an R Markdown document. Markdown is a simple formatting syntax for authoring HTML, PDF, and MS Word documents. For more details on using R Markdown see <http://rmarkdown.rstudio.com>.

When you click the **Knit** button a document will be generated that includes both content as well as the output of any embedded R code chunks within the document. You can embed an R code chunk like this:

```{r cars}
summary(cars)
```



```{r pressure, echo=FALSE}
plot(pressure)
```

